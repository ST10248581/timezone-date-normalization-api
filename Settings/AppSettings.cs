namespace DateInputNormalizer.Settings
{
    public static class AppSettings
    {
        // UTC: "UTC"
        // South Africa:    "South Africa Standard Time" (Windows) / "Africa/Johannesburg" (IANA)
        // New York:    "Eastern Standard Time" / "America/New_York"
        // London:  "GMT Standard Time" / "Europe/London"
        public const string ServerTimeZoneId = "UTC";
        public const string DefaultTimeZoneId = "UTC";
    }
}
